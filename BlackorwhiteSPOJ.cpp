#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    cout<<(1111/239)<<"\n";
/*    int i, j;
    while (true)
    {
        int n;
        cin>>n;
        if (n==-1)
        {
            break;
        }
        int a[n+3];
        for (i=0; i<n; i++)
            cin>>a[i];
        bool Bool[n+2];
        for (i=0; i<n; i++)
            Bool[i] = false;
        int left[n+1];
        for (i=0; i<n; i++)
            left[i] = 1;
        
        for (i=1; i<n; i++)
        {
            for (j=i-1; j>=0; j--)
            {
                left[i] = max(left[j] + 1, left[i]);

            }

        } 






    }*/



    return(0);
}